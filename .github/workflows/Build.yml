# ========================================================================
# DEPRECATED - Consolidated into .github/workflows/ci.yml
# ========================================================================
# This workflow has been merged into the main CI pipeline.
# 
# Docker build and SonarQube analysis are now part of ci.yml:
# - 'build' job: Builds Docker image (runs after lint, test, security, sonarcloud)
# - 'sonarqube-selfhosted' job: Optional self-hosted SonarQube analysis
#
# To enable self-hosted SonarQube analysis, set GitHub variable:
# - SONARQUBE_ENABLED = 'true'
#
# And configure these secrets:
# - SONARQUBE_HOST_URL: Your SonarQube server URL (e.g., http://sonarqube.local:9000)
# - SONARQUBE_TOKEN: Your SonarQube authentication token
#
# See: .github/workflows/ci.yml
# ======================================================================== 

name: DEPRECATED - Use ci.yml instead
on:
  push:
    branches:
      - __disabled__
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use .github/workflows/ci.yml"
